# アーキテクチャ — alteroid

この文書は [north_star.md](./north_star.md)（正典）と [PRD.md](./PRD.md)（要件）から導出した設計である。矛盾したら上位文書が勝つ。ここには「どう作るか」だけを書き、要件は書かない。

## 全体像

```
┌─ alteroidd（常駐デーモン）─────────────────────────────┐   ┌───────────────┐
│                                                          │   │ alteroid (CLI) │
│  クローン（Fable / SDK query・preset 一式）               │   │ = daemon の    │
│   ├ 道具: manager_start / manager_send / manager_list    │   │ 薄い HTTP      │
│   │       memory_* / journal_* / ask_human               │   │ クライアント    │
│   │       schedule_*（継続する依頼）ほか                  │   ├───────────────┤
│   │       （インプロセス MCP の自作ツール）                │   │ apps/web       │
│   │     ＋ 組み込み一式と人間の MCP 連携                   │   │ = React Router │
│   │       （委譲は方針。道具は削らない）                   │   │   の SPA。      │
│   ├ 受信箱（イベント駆動ループ）                          │   │   同じ API しか│
│   └ 記憶ストア ← **このプロセスだけが接続情報を持つ**       │   │   持たない      │
│                                                          │   └───────────────┘
│  スケジューラ / 外部イベント入口 / 承認待ちキュー           │
│  HTTP API（hono）─ chat(SSE)・jobs・承認                  │
│                    日報・日誌・セッションログ（可観測性3層）  │
│  RunnerRegistry ─ 委譲の宛先を決める間接層                 │
└──────┬───────────────────────────────────────────┘
       │ 命令: start / send / answer / stop / resume（HTTP）
       │ 出来事: デーモンが開いたストリームを流れ落ちる（SSE）
       │ **繋ぎに行くのは常にデーモン側。逆向きは張らない**
       │ （経路そのものが無いとは限らない → 下「runner API」）
┌──────▼────────────────────────────────────────────┐
│ manager-runner（別プロセス・既定では別コンテナ）             │
│  ・SDK セッションを N 本並行で持つ（人工上限なし）           │
│  ・**記憶ストアへ到達する鍵を持たない**                     │
│  ・待ち受けは Unix ソケットだけ（合鍵つき。TCP を開かない）  │
│  ├ マネージャー（Opus / tools 省略 = preset 全部）× N ← 別UID│
│  │  └ 作業者（Sonnet / agents 定義, tools 省略 = 全継承）× N │
│  │     workspace / MCP 設定 / OAuth トークンはここが持つ     │
│  └ 生ログは預けるだけ（永続化はデーモン）                    │
└───────────────────────────────────────────────────┘
```

## プロセスモデル

| 構成要素 | 実体 | モデル | ツール |
|---|---|---|---|
| クローン | デーモン内の長寿命 SDK セッション1本 | `fable` | **`tools` を渡さない**（preset 全部）＋インプロセス MCP の自作ツール＋人間の MCP 連携（`settingSources`） |
| マネージャー | **manager-runner の中で** SDK が spawn する子プロセス | `opus` | **`tools` を渡さない**（preset 全部。明示リスト禁止 — AGENTS.md の地雷1） |
| 作業者 | マネージャー配下の `agents` 定義 | `sonnet` | **`tools` フィールドを省略**（親の全ツールを継承） |

- **クローンにも道具は全部渡す。** 人間には Claude Code のほかに自分の手があり、それを取り上げた写像は人間の代替にならない（north_star「適用範囲」）。重い調査と実作業を下へ委ねるのは**方針**であって、`tools` を絞って実現しない
  - 委譲の原則はシステムプロンプトに書く。**「クローンが自分で手を動かした」は違反ではない** — 自分で見た方が早い場面で自分で見られることまでが写像である
  - 長寿命セッションの文脈を守る手当ては、道具の削除ではなく蒸留（記憶への移し替え）と委譲の方針で行う
- モデル対応（Fable / Opus / Sonnet）は固定。変更には人間の承認が要る（PRD）
- **マネージャーと作業者は実装物ではない。** どちらも実体は Claude Code そのものであり、alteroid が書くのは配線（起こす・話しかける・クローンへ回す・日誌に落とす）だけである
  - **作業者層の本体は `agents` 定義1個**（`model: 'sonnet'`、`tools` 省略）とマネージャーのシステムプロンプトに書く委譲の指針のみ。作業者用の独自機構（ワーカープール・キュー・独自プロトコル）を作らない。エスカレーションはサブエージェントの結果が親に返る SDK の挙動そのもの
  - この定義を省いて SDK の既定に頼ってはいけない。組み込みサブエージェントは**親のモデルを継承**するため、作業者が Opus で走ってコストが倍になり、固定のモデル対応が SDK の既定値変更で勝手に壊れる床に乗る（2026-08 調査: サブエージェントの既定モデルをセッション全体で指定するオプションは存在せず、`agents` の個別指定が唯一の方法）
- **「実体は Claude Code そのもの」は既定の構成についてである。** provider を差し替えれば、その層の実体はその provider の harness になる（要件は PRD「provider」）。**それでも alteroid が書く配線は変わらない** — 変わらないのは、境界を**1ターンぶんのストリーム**に引いてあるからである
  - **SDK の `query()` の署名を共通 IF にしないこと。** `AsyncIterable<SDKUserMessage>` + `Options` + `canUseTool` + control request は Claude 固有の制御モデルであり、これを IF にすると全 provider にその模倣を強いる（`queryFn` はテスト用の差し替え口として残す。provider の境界ではない）
  - 中立の語彙は `packages/core/src/agent-ports.ts`、`Options` の組み立ては `packages/core/src/claude-provider.ts` に閉じる。**前者から SDK を import しない**（番人テストで固定してある）
  - 「要件を担う能力」の機械可読な一覧は `agent-ports.ts` の `REQUIREMENT_BEARING_CAPABILITIES` が持つ。**要件の出所は PRD であって、こちらは写しである**（増減は PRD 側で決まる）
  - **境界は2つ要る。** マネージャーと作業者は1ターンぶんのストリームで足りるが、**クローンは自作の道具をインプロセス MCP（SDK の機能）で持っている**ので、道具を stdio MCP サーバへ外へ出すまで中立の口を作れない。無理に中立の顔を被せると SDK の型が `agent-ports.ts` へ漏れ、`queryFn` を別名で作り直すだけになる

## プロセス境界 — なぜ manager-runner を分けるか

**同じ器で走らせている限り、マネージャーは記憶ストアの鍵に届く。** デーモンの環境変数は
`/proc/1/environ` から読めるし、同一 UID なら人格データのファイルも読める。「非対称な
可視性」を掲げながら同居させるのは、鍵を机の上に置いて「見ないでください」と書くのと同じ
である。ツールを削って塞ぐのは禁止されている（north_star 禁止2）ので、**実行環境を分ける**。

| プロセス | 持つもの | 持たないもの |
|---|---|---|
| デーモン（alteroidd） | クローン、記憶ストアの接続情報、日誌・ジョブ・生ログの永続化、承認待ちキュー | 実プロジェクトの workspace、マネージャーの SDK セッション |
| manager-runner（本体） | SDK セッション（マネージャー＋作業者）、workspace、MCP 設定、モデル呼び出しの認証、制御面の合鍵の**ハッシュ** | **記憶ストアの接続情報、人格データ、デーモンの API を叩く資格、合鍵そのもの**（デーモンへの*経路*が無いと言えるかは構成による → 「runner API」） |
| マネージャー・作業者（runner の中の子プロセス。**別 UID**） | 人間が Claude Code に持たせるのと同じ道具一式、workspace | **runner 本体の環境、制御面のソケットと合鍵**（自分の許可確認に自分で答えられない） |
| PostgreSQL | 記憶・日誌・ジョブ・セッションの生ログ | — |

- **runner は判断をしない。** できるのは SDK セッションの start / send / stop / resume と、
  その出来事の返送だけである。許可してよい行為の一覧も、確認の要否の設定も持たない
  （権限境界はクローンが記憶を根拠に決めるもの — PRD「権限境界」）
- **runner は独自のエージェント基盤を持たない。** ワーカープールもキューも作らない。
  作業者は SDK の `agents` 定義1個のままである
- **runner に DB を持たせない。** 生ログ（SessionStore）も日誌も、runner は「預ける」だけで、
  永続化するのはデーモンである。runner に DB 認証情報を置いた瞬間、その中の子プロセスが
  同じ鍵に届く

### runner API — 命令は下り、出来事は上る

| 向き | 経路 |
|---|---|
| デーモン → runner | `POST /managers`（start） / `POST /managers/:id/resume` / `POST /managers/:id/messages` / `POST /managers/:id/answers` / `DELETE /managers/:id` / `GET /managers` / `GET /managers/:id/transcript` / `GET /health`（runner_id を名乗る） |
| runner → デーモン | `GET /events`（SSE）。`session` / `project_key` / `report` / `ask` / `settled` / `tool_use` / `mirror`（生ログ） / `archive` / `closed` |

**`/livez` 以外はすべて合鍵（Bearer）を要求する。** 下の「制御面の保護」のとおり、この口は
マネージャーが走っている器の中にあるので、鍵の無い呼び出しを通すと権限境界が迂回できる。

**接続を張るのは常にデーモン側である。** 逆向きのコールバック URL を足すと、runner の中の
子プロセス（＝マネージャー）がその経路でデーモンの API へ、つまり記憶へ届くようになる。
**この向きを反転させないこと。**

**ただし「経路が無い」と言い切れるかは構成による。** compose では runner とデーモンを別
ネットワークに置き、デーモンは 127.0.0.1 でしか待たないので、runner はデーモンの所在も鍵も
知らない。いっぽう **Railway の公開構成**（外から HTTP API を叩けるようにしたとき）では、
`*.railway.internal` が環境ごとにフラットで、かつ外から届かせるためにデーモンが
`ALTEROID_BIND=::` で待つので、**runner からデーモンへ TCP が届く**。実測（2026-08-14、
マネージャーのプロセスから）: `GET /health` は 200（認証を要求しない経路）、`/memory`
`/journal` `/managers` `/profile` はいずれも 401。

- **そこで守っているのはログイン認証だけである。** アクセストークンはデーモン側のストアに
  あり、`ALTEROID_GOOGLE_*` は runner へ配っていないので、マネージャーにはトークンを得る
  経路が無い。`/auth/*` は開いているが、その先は人間の `access grant` を通らないと何も持てない
- **これは受け入れた劣化であって、設計の意図ではない**（2026-08-14、人間の判断）。外から
  叩かせるには待ち受けを開けるほかなく、開くのは**認証が立つときだけ**である。公開しない
  構成では 127.0.0.1 のままで、この経路は開かない
- **失った守りを書いておく。「経路が無い」は多層防御の1枚だった。** 経路が無ければ、認証に
  穴が空いても記憶へは届かない。公開構成ではその1枚が抜けて**認証だけが砦になっている** —
  デーモンの認証のバグは、compose なら「外の誰かに開く」で済むが、そこでは**マネージャーが
  記憶へ届く**ところまで開く。読む者はこの前提で認証の変更を見ること
- 実測と運用上の内訳は [railway/README.md](../railway/README.md)「先に読む」4

- デーモンが繋いでいない間の出来事は runner 側に溜まる。捨てると、誰も答えられない確認が
  runner に残り、マネージャーが永久に待つ
- デーモンの停止は runner のセッションを止めない。**デーモンの都合で人の仕事を殺さない** —
  再起動後は生きているセッションへ繋ぎ直し、runner ごと落ちていた分だけ resume する

### 制御面の保護 — マネージャーは自分の許可確認に答えられない

runner API はマネージャーにとって**自分が走っている器の中にある**。ここに手が届くと、
`GET /managers` で自分の `requestId` を調べ、`POST /managers/:id/answers` に `allow` を
送って、クローンも人間も通らずに権限境界を迂回できる。他のマネージャーへの指示・停止・
生ログ閲覧も同じである。**「マネージャーから見たユーザーはクローン」という配線は、
制御面が触れる時点で成立していない。**

塞ぎ方は3枚重ねで、どれも能力の削除ではない（マネージャーの道具は1つも減らない）。

| 枚 | 何を | なぜそれで塞がるか |
|---|---|---|
| 1 | **TCP の口を開かない**（待ち受けは共有 volume 上の Unix ソケットだけ） | `curl http://127.0.0.1:4518` の宛先が存在しない |
| 2 | **ソケットは mode 0600・所有者はデーモンの UID** | 別 UID の子プロセスは connect できない |
| 3 | **runner のプロセスには合鍵の sha256 しか残さない** | 環境を読めても鍵は作れない。鍵が無ければ 401 |

3枚目が要るのは、1・2 が UID の分離に依存しているからである。**SDK の子プロセスは
runner 本体とは別の UID で走らせる**（`spawnClaudeCodeProcess` で降ろす。降ろす特権が
要るので runner 本体は root、降りた先は非特権）。同じ UID なら子は `/proc/1/environ` を
読み、ソケットにも繋げてしまう。

- **素の鍵を runner のプロセスに残さないこと。** 残った瞬間、3枚目が1・2 と同じ前提に
  戻る（今回避けたはずの `/proc/1/environ` がまた効く）
  - **人間が置く値は app と runner で同じでよい。** 器の起動スクリプトが、`exec` の
    前に sha256 へ畳んで素の値を環境から落とす。守りは「配り方」ではなく
    「**走っている runner が何を持っているか**」で決まる。二重管理（素の値と
    ハッシュを人間が別々に置く）は、ずれた瞬間に 401 が出続けるだけで1枚も守らない
- 生存確認だけの口（`/livez`）は鍵を要求しないが、制御面の情報を何も返さない
- 逆に、**鍵を持つデーモンからは全部通る**。塞いだのは能力ではなく本人確認である

### ローカルでは同一プロセスに落ちる（既知の穴つき）

`alteroid chat` を叩くだけで使える、というローカルの体験に「先に runner を立てる」手順を
足さない。runner の URL が無ければ、デーモンは同じプロセスの runner を使う。**このとき
既知の穴（マネージャーが同じ UID で走る）は残る。** 穴の名前を変えただけの状態なので、
ツール削除で塞がないこと。塞ぐのはコンテナ構成の役目である。

デーモンから見た顔はどちらも同じ（`RunnerClient`）。**デーモンは特定の runner 実装も、
固定 URL も、runner のローカルパスも前提にしない。**

## 同時実行の宛先 — RunnerRegistry

委譲の宛先は `RunnerRegistry`（`list` / `get` / `select`）を通して決める。M4 で登録されるのは
1台（`runner-primary`）で、`select` は常にそれを返す。間接層を最初から置くのは、宛先の決定が
呼び出し側に散らばると M5（複数 runner）で全部書き直しになるからである。

- `select` に**人工的な上限を入れない**。「同時に何本まで」は能力の削除であって配置の判断では
  ない（禁止2）。将来ここで見てよいのは、runner が報告する CPU・メモリ・稼働セッション数と
  いった**実行環境の資源**である
- 走行中のマネージャーの宛先は JobStore に残る（下の対応関係）。`manager_send` は
  `manager_id` から runner を引いて届ける（sticky routing）

## クローンの寿命モデル — 同一性はセッションではなく記憶に宿る

「長寿命セッション」は「不死身のログ」という意味ではない。クローンには寿命の違う3つの層がある。

| 層 | 実体 | 寿命 |
|---|---|---|
| 文脈（コンテキストウィンドウ） | セッションが「頭に載せている」もの | 有限。SDK の自動 compaction で周期的に要約へ潰され、細部は消える |
| トランスクリプト（JSONL） | resume 用の会話の生ログ | ただの再開素材。アーカイブして捨ててよい |
| 記憶（PersonaStore）＋日誌（JournalStore）＋継続する依頼（ScheduleStore）＋引き受けた仕事（CommitmentStore） | 蒸留された価値観・学び／追記専用の記録／時刻が来たら起こす依頼／まだ片付いていない仕事 | **この4つだけが一生モノ** |

- **クローンの同一性は記憶に宿る。** セッションは compaction されても、デーモン再起動で失われても、記憶＋日誌から再構成すれば同じクローンである。セッションの不死身化に工数をかけないこと
- **継続する依頼も一生モノである。** 「定期的に〜しておいて」は記憶（根拠）とは別に、時計を持つ器で保つ（PRD「自律」）。ここが揮発すると、人間が一度頼んだ仕事が再起動や compaction で静かに消える — 自律の側が壊れるので、記憶と同じ寿命に置く
- **引き受けた仕事も一生モノである。** 「返事はしたが、まだやっていない」はセッションの文脈にしか無い状態で、compaction でも再起動でも消える。継続する依頼と同じ理由（PRD「自律」）で、記憶と同じ寿命に置く
- **蒸留は生存条件である。** 大事なことはセッションが忘れる前に記憶へ移す。蒸留を怠ると compaction のたびに人格の一部が消える構造なので、「あると嬉しい機能」として後回しにしない（会話終了時に加え、compaction 前にも蒸留の機会を置く）
- **監査は日誌＋アーカイブで担保する。** PreCompact フックで要約に潰す直前の全文をアーカイブへ落とす。人間が後から追う用途にセッション本体を太らせ続けない

## 同時実行モデル — クローンは複数のマネージャーを使う

人間が Claude Code を複数枚開くのと同じことが、クローンにもできる。

- **委譲はノンブロッキング** — `manager_start(依頼, 作業Dir)` は起動して即 `manager_id` を返す。クローンは完了を待たずに次の判断へ移る
- **受信箱でイベント駆動** — 人間の発言・各マネージャーからの報告や質問・タイマー・外部イベントは、すべてクローンの受信箱に積まれる。クローンは一件ずつ取り出して判断する。これが自律要件（起点4つ）の実装形態でもある
  - **例外は「返事を待っているあいだに積み上がった人間の発言」だけ** — 同じ会話の連続した発言は1ターンにまとめて読む。人間は返事を待ちながらも喋るので、1件ずつ別のターンで読むと、後で言い直された最初の一言に本気で答えてから次のターンでやり直すことになる（人間が Claude Code に立て続けに3行打ったときに起きるのはそれではない）
  - まとめても**捨てない**。全文を届いた順に渡し、要約も間引きもしない。合図そのものも1件ずつのまま残る（未読・台帳・日誌は件数ぶん）
  - **順序は並べ替えない。** 間に別の起点（タイマー・マネージャーからの一件・外部イベント）が挟まっていたらそこで止める。会話が違う発言、配り直しの合図、枠で保持した合図もまとめない
- **会話に戻れる** — `manager_send(manager_id, メッセージ)` で走行中のマネージャーに追加指示や質問への回答を送る。`manager_list()` で一覧と状態を見る
- **止められる** — `manager_stop(manager_id, 理由)` でその1本だけを止める。人間の停止（Web UI / `DELETE /managers/:id`）と同じ経路を通り、止まったかは runner のセッション一覧まで見て確かめる。人間に出来てクローンに出来ないことを作らない（north_star 禁止1）
- **数に人工上限を設けない** — 上限はマシンリソースそのもの（＝実行環境の境界）。同時数の設定値やキュー長で絞らない（追加制限禁止）
- **同一リポジトリの衝突も機構で禁じない** — 人間も同じ事故を起こせて、避けているのは判断である。クローンの判断（と記憶）に委ね、git worktree 等の隔離はマネージャーの技量とする

## 配線 — マネージャーから見た「ユーザー」はクローン

マネージャーのセッションで発生する `AskUserQuestion`・許可確認・最終報告を、デーモンがクローンの受信箱へルーティングする。人間 → Claude Code の対話の仕組みが、SDK の機能そのままでクローン → マネージャーに写る。

- クローンは記憶に根拠があれば自分で答え、無ければ `ask_human` で承認待ちキューに積む（PRD「権限境界」の実装点はこの1箇所）
- 承認待ちで止まるのは**その仕事だけ**。他のマネージャーは走り続ける（PRD「自律」）
- 作業者 → マネージャーのエスカレーションは SDK のサブエージェント機構そのもの（結果が親に返る）を使い、独自機構を足さない

エスカレーション・報告・日誌エントリは zod スキーマの型付きメッセージとして `core` に定義する（提供価値2）。

## 非対称な可視性

**下向き（クローン → マネージャー以下）: 同じものが見える**

- マネージャーには人間が使っているのと同じ MCP 設定を渡す（`settingSources` で `.mcp.json` を共有。SDK 公式機能）
- cwd は実プロジェクトの作業ディレクトリ。人間が Claude Code を開く場所と同じ
- **クローンは2通りで見る。** 普段はマネージャー越しに見る（人間が Claude Code に任せるのと同じ）。加えて**自分の道具でも直接見られる** — 人間が Claude Code に頼まず自分でブラウザや端末を開くのと同じ写像である（north_star「適用範囲」）
  - 人間が使っている MCP 連携はクローンからも使える（PRD「業務範囲」の要件）。クローン側も `settingSources` で人間の設定を読む
  - **どちらで見たかは日誌に残す。** 委譲が原則である理由（俯瞰と判断を守る）が守られているかは、禁止ではなく記録で見る

**クローンが自分の手を持つことの帰結（記憶ストアへの直接到達）**

クローンはデーモンプロセスの中に居るので、道具を持つと記憶ストアの接続情報にも到達できる。**これは境界の破れではない** — 記憶はクローン自身のものであり、上向きに守っているのは「マネージャー以下から見えないこと」である。ただし次を設計の前提として置く。

- 記憶の更新は `memory_*` を通すのが**方針**である（日誌に残り、蒸留の経路に乗るため）。DB を直接書ける状態を塞ぐのではなく、方針とシステムプロンプトで表す
- したがって**マネージャーを同じプロセスに戻してはいけない**。クローンが道具を持つ構成では、記憶ストアの鍵を持つプロセスに道具のある層が同居する形になる。runner を別プロセス・別コンテナに保つことが、上向きの守りの本体になる

**上向き（マネージャー以下 → クローン）: 見えない・書けない**

- 記憶ストアの接続情報（ローカルならパス、クラウドなら DB 認証情報）は**デーモンプロセスだけが持つ**
- マネージャーは manager-runner の中で走る。runner には鍵を配らないので、構造的に触れない
- ツールを絞って実現するのではなく、認証情報の配布範囲と経路（＝実行環境の境界）で守る

クラウド構成では次の4つが同時に成り立って、初めて「到達経路が存在しない」と言える。**env から
消すだけでは足りない**（同じ器なら `/proc/1/environ` で読める）。

1. 別プロセス・別コンテナ（デーモンの環境も人格データも runner から見えない）
2. runner の環境変数にも設定ファイルにも DB 認証情報が無い
3. **runner から DB への経路が無い**（compose ではネットワークを分ける。runner は `db` を名前解決すらできない）
4. **マネージャー（子プロセス）は runner 本体とも別 UID** — runner の環境も、デーモンへ繋がる
   制御面のソケットも読めない。ここが同じ UID だと、1〜3 を積んでも制御面から迂回できる

**既知の穴（ローカル）**: 同一プロセスの runner に落とすローカル構成では、マネージャーが Bash で
`~/.alteroid/` を読むことは技術的に可能。SDK のサンドボックス設定は現状ドキュメント未記載
（2026-08 調査）。ローカルはプロセス境界＋監査（PostToolUse フックで全ツール実行を日誌に記録）で
受け、完全な強制はコンテナ構成で成立する。**この穴をツール削除で塞がないこと。**

## 実行環境プロファイル — 環境変数を器に増やさない

道具の鍵や `PATH` を「器の環境変数」として増やしていくと、**1つ足すたびに実装と器の定義を直す**ことになる（鍵の一覧・`compose.yaml`・Railway の Variables・`Dockerfile` のシム）。人間が自分の端末で `~/.zshenv` に1行足せば済ませていることが、この階層では実装作業になっている — それはデグレードであって設計ではない（north_star 禁止1）。

だから**実行環境そのものを1本のシェルスクリプトとして持つ**。人間の `.zprofile` / `.zshenv` に当たるもので、器は中身を解釈しない（`export` でも `eval $(...)` でも `PATH` の追加でもよい）。増えるのは人間が書く行であって、実装ではない。

- **置き場は記憶ストアと同じ器**（fs なら `~/.alteroid/profile.sh`、pg なら1行の表）。器を作り直しても残る
- **記憶ではない。** 人格は記憶（Markdown）に宿るのであって、鍵や `PATH` はそこに混ぜない。プロファイルの本文はクローンのシステムプロンプトに載らない
- **runner は自分で取りに行かない。** 取りに行けるということは runner に記憶ストアの鍵があるということで、それは上向きの守り（下記）が無いということである。**デーモンが制御面で降ろす**（`POST /runners/profile`）。名乗り（`hello`）のたびに降ろし直すので、器を作り直しても環境は痩せない
- **効く先はクローン・マネージャー・作業者のすべて。** 人間の `.zshenv` は「Claude Code に頼むとき」にも「自分で端末を叩くとき」にも効く。層で差を作らない
- **本命は SDK 子プロセス起動時の評価。** この env を継承した先でマネージャーも作業者も MCP サーバも走るので、**これから起こす仕事には差し替えが即座に効く**
- **走行中の仕事へ確実に届くのは `gh` シムの経路だけ。** `gh` と、`gh` から資格情報を借りる `git` は呼ばれるたびにプロファイルを読み直す（`credentials.ts` と同じ形）。`BASH_ENV` も渡してあるが、bash がこれを読むのはスクリプトとして起こされたときで、`bash -c`（stdin が端末でない）では読まない。加えて SDK の Bash ツールはセッションごとの永続シェルである。**走行中の全コマンドへ届くとは書かないこと**（保証を広げたいなら bash 側にシムを置く話になり、実機での確認が要る）
- **上（記憶）へ到達する鍵は、本文が何を `export` しても配らない。** シェルが何を export するかは静的に読めない（`eval` がある）ので、「書かせない」ではなく「最後に `unset` する」で守る。器が書く行・評価側の削除・合成順序の三重で塞ぐ
- **壊れたものは保存も配布もしない。** 構文を間違えたスクリプトを `BASH_ENV` に載せると、以後すべてのコマンドが壊れた環境で走り、原因はどこにも出ない。置く前に1度評価し、通らなければ理由を返す（前のものが残る）
- **更新は1本の列を通り、途中で落ちても旧版で揃える。** 評価 → 正本（記憶ストア）へ保存 → クローンの器へ反映 → runner へ配布、の順で、どの段で落ちても最後は旧版に戻す（保存できたのに反映できなければ、正本を書き戻す）。**「失敗を返したのに1層だけ新版」を残さない** — 残すと、次に runner が名乗った時点でそれが配られ、分裂が黙って広がる
- **クローン自身も読み書きできる**（`profile_read` / `profile_write`）。人間が「このトークンを使って」と言ったものを、会話の中に置いたままにせず実行環境へ移せる。人間が自分の `.zshenv` を直せる以上、その写像であるクローンにできないのは能力の削除である（north_star「適用範囲」）。鍵が文脈に載ることは**方針**（本文を記憶・日誌へ書き写さない）で扱い、道具を取り上げて表現しない
- **HTTP の口に触れるのは実行環境の持ち主だけ**（入口の認証で `/access/*` と同じ資格）。クローンの道具とは層が違う — あちらは記憶の持ち主が自分の実行環境を直す話で、こちらは「外から誰が叩けるか」の話である。プロファイルの差し替えは、記憶ストアの鍵を持つプロセスでの任意コマンド実行そのものである。「alteroid を使ってよい」（＝ログインして許可された）に、実行環境そのものを差し替える資格までは含めない。人間が `.zshenv` を直すのはその人が持っている箱の上である、という線引きと同じ
- `credentials`（`POST /runners/credentials`）を置き換えるものではない。あちらは「1つの鍵を、名前を検査したうえで回す」細い口で、こちらは実行環境そのものを丸ごと差し替える太い口である。効く順序は **credentials → profile**（人間が明示的に書いたほうが勝つ）

## 脳は1インスタンス

クローンの生きたインスタンスは常にデーモン内の1本だけ。CLI は core を埋め込まず、デーモンへの薄い HTTP クライアントに徹する（core を CLI にも埋めると chat のたびに脳が分岐する）。CLI と HTTP API はどちらも「脳への接続手段」であり、常駐が自律の前提（PRD）という要件とも一致する。

## ストレージ

core にストアのインターフェースを切り、ドライバを差し替える。

| インターフェース | 中身 | fs ドライバ（ローカル） | pg ドライバ（クラウド） |
|---|---|---|---|
| PersonaStore | 記憶（価値観・学び） | Markdown ファイル群 | PostgreSQL（同じ Markdown 文書をテーブルに） |
| JournalStore | 日誌（追記専用・型付きエントリ: やり取り、判断、エスカレーション、ツール実行、日報） | JSONL | PostgreSQL |
| TranscriptArchive | セッション生ログ（PreCompact 退避分を含む） | JSONL ファイル | PostgreSQL または S3 互換 |
| JobStore | ジョブ・承認待ち・下の対応関係 | JSON | PostgreSQL |
| ScheduleStore | 継続する依頼（名前・周期・依頼の本文・前回動いた時刻） | JSON | PostgreSQL |
| CommitmentStore | 引き受けたまま終わっていない仕事（起点・受け取った時刻・依頼の本文・片付いたか） | JSON | PostgreSQL |
| ProfileStore | 実行環境プロファイル（`.zprofile` 相当のシェルスクリプト1本） | `profile.sh`（0600） | PostgreSQL（1行） |

- **ScheduleStore は器の違いで能力差を作らない。** 片方では「定期的にやって」が効かない、が起きてはいけないので、上の層から見て省略できる存在にしない
- **CommitmentStore も同じ**（片方の器でだけ、着手しなかった依頼が黙って消える、を作らない）。**開く操作は冪等であること** — 未読の合図は配り直されうるので（下の受信箱の取引）、同じ id で二度開いたときに上書きすると、**器が落ちるたびに片付いた仕事が蘇る**
- **CommitmentStore に順序・優先度の列を足さないこと。** 足した瞬間に「何を先にやるか」の判断が器へ移り、PRD「自律」が禁じている「やることの一覧」になる。溜まっているものはクローンへ毎ターン件数と齢で渡し、順序はそのつど決め直させる
- **仕込みの真実はストア側だけに置く。** スケジューラへ直接足す口を作ると、デーモン再起動で仕込みが消える（スケジューラはストアを読み直すだけの側である）
- **発火の合図に依頼の本文を載せない。** 載せた瞬間に発火時点の写しになり、人間が本文を直しても古い依頼で走る。運ぶのは名前だけで、本文は処理する瞬間に読む

### JobStore が持つ対応関係 — 委譲を復元するための鎖

```
manager_id → runner_id → sdk session_id → workspace locator
```

- **`manager_id → runner_id`**: どの runner で走っているか。`manager_send` の宛先であり、
  デーモンが再起動しても affinity をここから復元する
- **`→ session_id`**: 中断されたセッションの続きへ戻る足がかり（`resume`）
- **`→ projectKey`**: SDK が生ログを預けるときの scope。生ログを後から引き当てる鍵
- **`→ workspace locator`**: どこで作業していたか。**文字列のパスだけにしない**（runner が
  増えれば移送の話になる）

```ts
type WorkspaceLocator =
  | { kind: 'runner-volume'; runnerId: string; path: string }  // M4 の既定
  | { kind: 'shared-volume'; path: string }                    // 共有 FS（M5）
  | { kind: 'git'; repository: string; ref: string; patchId?: string }; // 再構築（M5）
```

M4 で実際に使うのは `runner-volume` だけである。**runner のローカルパス（走行中の
トランスクリプトの置き場など）を台帳に持たないこと** — runner が入れ替わった瞬間に嘘になる。
生ログへは runner の API → アーカイブ → 預かったセッションの生ログ、の順で降りる。

- **fs を先に作る**。記憶が Markdown ファイルであることは「人間がいつでも読んで直せる」（提供価値1）の最短の実装
- SDK セッション自体の永続化は SessionStore アダプタ（SDK 公式）で同じ PostgreSQL に載せる。デーモン再起動時は JobStore の session_id からマネージャーを resume する

## パッケージ構成

```
packages/core        ドメイン全部: クローンループ、委譲（デーモン側）、runner 側の
                     SDK セッション、境界のプロトコル(zod)、ストアIF
packages/storage-fs  ローカル用ドライバ（Markdown / JSONL / JSON）
packages/storage-pg  クラウド用ドライバ（PostgreSQL / drizzle）
apps/daemon          alteroidd = core をホストする常駐プロセス + HTTP API（hono）
apps/runner          alteroid-runner = manager-runner。SDK を隔離して走らせる
apps/cli             alteroid = daemon への薄いクライアント（hono/client で型共有）
apps/web             公式の画面。React Router v7 の SPA。@alteroid/api-client 経由で
                     デーモンの API だけを見る（独自の経路を持たない）
packages/api-client  生成 spec から起こした外部向けクライアント。apps/web が最初の消費者
```

境界の両側が `core` に居るのは、**プロトコルと型を1か所に置くため**である。実際に動くときは
別プロセス（既定では別コンテナ）で、runner 側のコードはデーモンのストアに触れない
（`Stores` を受け取らない）。

## 技術選定

| 領域 | 選定 | 理由 |
|---|---|---|
| ランタイム | Node 22 LTS | SDK（要 Node 18+）が子プロセスを spawn する関係で Bun は採らない |
| モノレポ | pnpm workspaces | 標準的。SDK と相性問題なし |
| エージェント基盤 | @anthropic-ai/claude-agent-sdk | 自作しない（AGENTS.md） |
| HTTP | hono | 軽量、SSE 対応、`hono/client` で CLI と型を共有 |
| スキーマ | zod | エスカレーション・プロトコルの型付きメッセージの実体 |
| DB（クラウド段） | PostgreSQL + drizzle | マイグレーション込みで軽量 |
| テスト | vitest | — |
| CLI | commander + SSE ストリーミング | chat は readline ベースで開始。TUI 化は後から可能 |

## Web UI — 画面とデーモンのオリジンが違うこと

要件は PRD「インターフェース」にある。ここには設計判断だけを書く。

**SPA（`ssr: false`）である。** 画面のためのサーバを増やさない。デーモンをどこに置き画面を
どこに置くかは人によって違う（同一ホスト / `api.example.com` + `www.example.com` /
デーモンは自宅で画面は静的ホスティング）ので、**サーバを持たない静的成果物**ならそのどれにも
同じものを置ける。SSR にすると「画面を動かすための実行系」が増え、置ける場所がそれを持てる
ところに縮む。

**接続先はビルドに焼き込まない。** 上から順に、①人間がこの画面で設定した値（`localStorage`）
②ビルド時の `VITE_ALTEROID_API_URL` ③同一オリジンの `/api`。①があるので、公式が配る成果物
1つのまま人によって違うデーモンへ向けられる。

**資格情報は Cookie ではなくヘッダで運ぶ。** 別ドメイン間の Cookie は成立しない —
`SameSite=None; Secure` はサードパーティ Cookie の廃止と ITP で消えていく経路であり、
`www.hoge.vercel.app` と `api.example.com` のように**登録可能ドメインが違えば `Domain` 属性で
共有することもできない**。ヘッダならオリジンに縛られないのでどの配置でも同じように動き、かつ
**単純リクエストでは付けられない**ので CSRF が構造的に成立しない。デーモン側も
`Authorization: Bearer` しか見ない（`credentials` は返さない）。

**ログインは CLI とまったく同じ経路を通る。** `POST /auth/login` が返す `claimSecret` は
呼んだ相手に渡されるもので、端末に固有の仕掛けは無い。だから画面も同じように
`POST /auth/login/:id/claim` で引き取れる — **画面のためにデーモンへ足した経路は1本も無い**
（PRD「入口の等価性」）。コールバックは「端末に戻れ」と書いた HTML を返すだけで鍵を渡さない
ので、始めたタブが生きている必要がある（ポップアップ＋`sessionStorage` の預かり）。

**「未ログイン」と「許可が無い」を混ぜない。** 前者は 401 でログインし直せば解決するが、
後者は 403 で、**何度ログインしても解決しない**（許可は人間が `alteroid access grant` で
与えるものだから）。混ぜると、直しようのない導線を回し続ける画面になる。同じ理由で、
401 では鍵を捨てるが 403 では捨てない。

**CORS は既定で閉じている。** `ALTEROID_ALLOWED_ORIGINS` に**明示列挙**したオリジンだけを
そのまま返す（ワイルドカードは受け付けず、`credentials` も返さない）。既定は空で、そのとき
デーモンは CORS ヘッダを一切返さない — つまり**既定の姿勢は今までと1バイトも変わらない**。

**CORS は認証より先に登録する。** ブラウザの preflight（OPTIONS）は `Authorization` を積んで
来ないので、門番が先に立つと preflight が 401 になり、本リクエストが一度も飛ばない。
`allowHeaders` に `authorization` を入れるのも同じ理由で、落とすと**ログインだけ成功して
以降の全経路がブラウザに止められる**（`/auth/login` と `/claim` は `content-type` しか要らない
ので、いちばん分かりにくい壊れ方をする）。

ここを緩めてはいけない理由が `deliberateClient`（`apps/daemon/src/app.ts`）にある。127.0.0.1
で待つことはブラウザからの保護にならず、人間が開いた任意のページが単純リクエストを投げられる。
本文検査の無い POST に `application/json` を要求すると preflight が必須になり、**CORS ヘッダを
返さないから preflight が通らない**という形で塞いでいる。`*` を返した瞬間にこの前提が消え、
他人がクローンのターンを起こせる状態に戻る。開けるかどうかは人間が決め、開けた先は列挙した
相手だけに限る — これが方針ではなく**実行環境の境界**で表すということである（north_star 禁止2）。

開発中は Vite の proxy（`/api` → デーモン）で同一オリジンに見せる。**開発のためだけに CORS を
開けさせない。**

## 認証

- ローカル: Claude サブスクリプション（OAuth）。人間が `claude` でログイン済みの認証を SDK が使う
- クラウド / コンテナ: `claude setup-token` の長期トークンを `CLAUDE_CODE_OAUTH_TOKEN` としてシークレット注入（公式サポート。2026-08 確認）
### デーモンの API に入る資格

**既定では要求しない。** `ALTEROID_GOOGLE_CLIENT_ID` と `ALTEROID_GOOGLE_CLIENT_SECRET` が揃うと自動で有効になり、`ALTEROID_AUTH` で明示的に開閉できる。設定していない人の `alteroid chat` が突然通らなくなるのは、境界の導入が実質のデグレードになる典型である（[north_star「立ち戻るための問い」](./north_star.md)の最後）。

通る資格は2種類ある。

| 資格 | 誰が持つ | できること |
|---|---|---|
| アクセストークン（`alt_…`） | `alteroid login` した端末・画面・外部アプリ | 許可されていれば API 全般 |
| 実行環境の持ち主のトークン | `state/daemon.json` を読める者 | API 全般 ＋ `/access/*`（許可の付与） |

- **ログインしただけでは使えない。** 使う許可は人間が `alteroid access grant` で与える。これは PRD「権限境界」とは別の層である — あちらは「クローンが何を人間へ確認するか」を記憶で決める話で、こちらは「そもそも誰が API に触れるか」であり、持つのは許可されているか否かの2値だけ（行為の一覧を持たない）
- **入口ごとに認証を作らない。** CLI・HTTP API・Web UI は同じ門番を通る（PRD「インターフェース」）
- 資格は `Authorization: Bearer` だけで運ぶ。Cookie は受けない（[Web UI](#web-ui--画面とデーモンのオリジンが違うこと)の項）
- **CORS はブラウザにしか効かない。** `curl` は素通りするので、外から届く場所に置くならここを有効にするか、手前に境界（リバースプロキシ・トンネル）を置くこと。認証を切ったまま公開しないこと

## 実装フェーズ

| フェーズ | 内容 |
|---|---|
| M0 | モノレポ雛形 + CI |
| M1 | `init` / `chat` — デーモン内クローン + fs 記憶 + 会話終了時の蒸留。**この時点からデーモン常駐**（chat はデーモンに接続する形。常駐は後から足す機能ではない — PRD） |
| M2 | 委譲 — マネージャー起動（複数並行）、作業者、エスカレーション配線、日誌 |
| M3 | 自律 — スケジューラ、外部イベント、クローンの発意、承認待ちキュー |
| M4 | クラウド — pg ドライバ、SessionStore、**daemon / manager-runner の分離**、Dockerfile + compose（3コンテナ）、setup-token |
| M5 | 複数 manager-runner と水平スケール — runner の登録・生存判定・配置、runner 障害時の再開 |
| M6 | Web UI — React Router v7 の SPA（`apps/web`）。CLI と等価な入口を API の上に用意する |

**`docs/roadmap.md` は 2026-08-22 に削除した**（人間の判断）。人間の言葉をそのまま引く:

> 直していいですよ。というかそもそもroadmap管理をそろそろやめたいんですよ。開発を始めた頃はドキュメントという形でわかりやすく書き起こして進めていましたが、現在はalteroidが実際に動くようになり、タスクはそちらに積まれるようになったので不要だと思っています。

**M5 / M7 の未完項目はクローンの台帳へ移した**——`commitment_list` / `commitment_open` / `commitment_close`。`packages/core/src/prompt.ts:122` に「引き受けたまま終わっていない仕事の台帳。**順序は持たない**」と在る（何を先にやるかは実装する側が決める）。

**受け入れ基準・地雷・順序の依存の逐語は `git show <sha>:docs/roadmap.md` で読める。** sha は2つあり、指す先が違う。

- `git show fa98bb2:docs/roadmap.md` — **roadmap.md を最後に更新したコミット**（2026-08-21、#138）。そこから削除の時点まで中身は1バイトも変わっていないので、M5・M7 を読むならこちら。**畳む側の PR の sha ではなく main に在る sha を指しているのは意図である** — 畳む PR は rebase で書き換わるので、そこを指すと出所ごと消える
- `git show 7046e2c:docs/roadmap.md` — **前日（2026-08-21）の削除の直前のリビジョン**。M0〜M4・M6 を読むならこちら（この時点で済んだフェーズの分は既に roadmap.md から消えていた）

**以後、上の実装フェーズ表が M0〜M6 の識別子の定義の在り処である。** 参照するコード注釈・スクリプトは識別子（M5・受け入れ基準N・PRn）だけを残し、`roadmap` の語は落としてある。**M7 はこの表にまだ無い**——roadmap.md にしか存在しなかった新しいフェーズで、コード注釈からの参照も無かった（`grep -rn 'M7'` で確認済み、docs/roadmap.md 以外にヒットなし）。M7 の中身は上の2つの `git show` でしか読めない。

## 未解決事項

- SDK のサンドボックス設定が公開されたら、ローカル（同一プロセス runner）の「既知の穴」を
  それで塞げるか再評価する
- runner を跨いだ workspace の移送（共有 FS か git 再構築か）は M5 で決める。M4 では
  locator を残すところまで
- 外部イベントの入口の形（webhook を受ける HTTP エンドポイント / MCP 経由のポーリングジョブ）は M3 で決める
- クローンの compaction で要約に何を残すか（走行中のマネージャー一覧・未処理の受信箱は消えると困る）は M1 の実測で決める。原則は「寿命モデル」のとおり: 残すべきものはそもそも要約ではなく記憶と JobStore に置く
