packages:
  - packages/*
  - apps/*

allowBuilds:
  esbuild: true

catalog:
  # typescript-eslint が TS 7（ネイティブコンパイラ）未対応のため 6 系に固定（peer: >=4.8.4 <6.1.0）
  typescript: ^6.0.3
  tsup: ^8.5.1
  # CI の実行系（Node 22）に合わせる
  '@types/node': ^22.20.1
