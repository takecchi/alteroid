packages:
  - packages/*
  - apps/*

allowBuilds:
  esbuild: true

catalog:
  # typescript-eslint が TS 7（ネイティブコンパイラ）未対応のため 6 系に固定（peer: >=4.8.4 <6.1.0）
  typescript: ^6.0.3
  tsup: ^8.5.1
  # root の `format:check` と apps/daemon の write-openapi.mjs（openapi.json を
  # 整形して書き出す）が同じ版を使う必要がある。片方だけ上がると整形結果がずれ、
  # 「コードは変えていないのに openapi.json に差分が出る」で CI が振れる。
  prettier: ^3.9.6
  # CI の実行系（Node 22）に合わせる
  '@types/node': ^22.20.1
  '@anthropic-ai/claude-agent-sdk': ^0.3.268
  hono: ^4.13.5
  '@hono/node-server': ^2.1.0
  '@hono/zod-validator': ^0.9.0
  zod: ^4.4.3
  commander: ^15.0.0
  # 継続中の依頼の周期。人間が cron で書けることをこの階層でも書けるようにする。
  # 依存ゼロ・ESM・型付きなので croner を採った（cron-parser は luxon を連れてくる）
  croner: ^10.0.1
  # OpenAPI（外から API を記述できるようにするための一式。Issue #20）
  #
  # 生成側は hono-openapi。既存のチェーン記法と `AppType`（CLI の `hc<AppType>` が
  # 依存している）をそのまま保てるため、@hono/zod-openapi（全ルートを createRoute へ
  # 書き換える必要がある）は採らなかった。
  hono-openapi: ^1.3.1
  '@hono/standard-validator': ^0.4.0
  '@standard-community/standard-json': ^0.3.5
  '@standard-community/standard-openapi': ^0.2.9
  'openapi-types': ^12.1.3
  # 人間が読む画面（GET /docs）
  '@scalar/hono-api-reference': ^0.11.13
  # 外部向けの生成クライアント（fetch ベース・軽量）
  'openapi-typescript': ^7.13.0
  'openapi-fetch': ^0.17.0
  # クラウド段（M4）の記憶ストア
  drizzle-orm: ^0.45.2
  pg: ^8.23.0
  '@types/pg': ^8.21.0
  # pg ドライバのテスト用。実 PostgreSQL をインプロセスで動かす（CI に DB を要求しない）
  '@electric-sql/pglite': ^0.5.4
  # Web UI（apps/web）。React Router v7 を SPA モード（ssr: false）で使う。
  #
  # **SSR を採らないのは配置の自由のため。** デーモンをどこに置き、画面をどこに置くかは
  # 人によって違う（同一ホスト / api.example.com + www.example.com / 静的ホスティング）。
  # サーバを持たない静的成果物なら、そのどれにも同じものを置ける。
  react: ^19.2.8
  react-dom: ^19.2.8
  '@types/react': ^19.2.18
  '@types/react-dom': ^19.2.4
  react-router: ^7.18.2
  '@react-router/dev': ^7.18.2
  # SPA でもビルド時に要る（index.html を書き出す側が実行系を要求する）。
  # 成果物は静的なので、これが動くのはビルドの中だけ。
  #
  # **どちらも `dependencies` に置くこと。** react-router のビルドは
  # `package.json` の `dependencies` だけを見て実行系を判定し、`isbot` が
  # 無いと**ビルド中に package.json を書き換えて `pnpm install` を走らせる**。
  # CI でそれが起きると、成果物の生成が依存関係の書き換えを伴う。
  '@react-router/node': ^7.18.2
  isbot: ^5.2.1
  vite: ^8.2.1
  tailwindcss: ^4.3.3
  '@tailwindcss/vite': ^4.3.3
  # 取得とキャッシュ。SSE で届いた出来事から `mutate` で無効化する使い方をする。
  # 2.5 系は出たばかりで minimumReleaseAge に引っかかる。**除外リストに足して
  # 通すのではなく**、枯れている 2.4 系を採る（除外リストは Claude 本体＝SDK を
  # 遅らせないためのものであって、一般の依存の新しい版を素通しするための場所では
  # ない。理由はファイル末尾の注記）
  # `^` だと 2.5 系まで入ってしまうので `~` で 2.4 系に留める
  swr: ~2.4.2
  clsx: ^2.1.1
  # Web UI（apps/web）で AI の応答・日報・マネージャーの報告を Markdown として
  # 描画する（人間の依頼: 「AIの返答ってMarkdown返却多いからWebUIも表示を
  # Markdownにしたほうがこっちとしては見やすい」）。
  #
  # react-markdown を採ったのは、remark（Markdown→mdast）→ remark-rehype
  # （mdast→hast）→ hast-util-to-jsx-runtime（hast→React 要素）で完結し、
  # **`dangerouslySetInnerHTML` も HTML 文字列も経由しない**ため。HTML 文字列を
  # 作ってから貼る方式（`marked` + `dangerouslySetInnerHTML` など）は、サニタイズを
  # 足し忘れた瞬間に注入経路になる。react-markdown はその経路自体が無い。
  # remark-gfm は表・取り消し線・タスクリストなど GFM 拡張のため（AI も日報も
  # 表を返すことがある）。
  #
  # remark-breaks も併せて入れる。素の Markdown は単独の改行を畳んで表示する
  # （半角スペース2つや空行での改行と区別しない）。この画面はこれまで
  # `whitespace-pre-wrap` で改行をそのまま見せていたので、remark-breaks が
  # 無いと「今まで見えていた行区切りが消える」という劣化になる。
  #
  # **`rehype-raw` は入れない。** 生の HTML を hast へ解釈し直す道具で、これを
  # 足した瞬間に本文中の `<script>` や `onerror` 付きタグがそのまま要素として
  # 解釈される注入経路が生まれる。足したくなったら
  # `apps/web/app/components/markdown.tsx` の同じ注記と、
  # `apps/web/app/components/markdown.test.tsx` の「生 HTML が要素にならない」
  # テストを先に見ること。
  'react-markdown': ^10.1.0
  'remark-gfm': ^4.0.1
  'remark-breaks': ^4.0.0
  # 画面の回帰テスト（描画して操作する分だけ。DOM が要るファイルにだけ jsdom を指定する）
  jsdom: ^30.0.1
  '@testing-library/react': ^16.3.2
  '@testing-library/dom': ^10.4.1
  tailwind-merge: ^3.6.0
  lucide-react: ^1.31.0
  # shadcn/ui（apps/web）。**既存の部品を置き換えるためではない** — 足回り（clsx /
  # tailwind-merge / cn / lucide-react）は上に既にあり、shadcn を入れて手に入るものは
  # もう手元にある。入れているのは**自前では書きたくない振る舞いを持つ部品**（いまは
  # sheet ＝ Radix Dialog。焦点の閉じ込め・スクロールの固定・`aria-modal`）のためである。
  #
  # `radix-ui` は統合パッケージ（`@radix-ui/react-*` を個別に入れない）。shadcn の
  # radix 系スタイルが `import { Dialog } from 'radix-ui'` の形で吐くので、これに揃える。
  'radix-ui': ^1.6.7
  # shadcn が吐く部品の variant 定義（`cva`）。
  'class-variance-authority': ^0.7.1
  # `animate-in` / `slide-in-from-left-*`（sheet の出入り）。**色は1つも持たない**
  # （中身は @property と @utility だけ）ので、既存のパレットには触れない。
  'tw-animate-css': ^1.4.0
  # `@import 'shadcn/tailwind.css'` の実体（`data-open` / `data-closed` などの
  # custom variant。sheet の class がこれを前提にしている）。**dependencies に置く**
  # のは、これが CSS の import 元だからである。同じものが `shadcn add` の CLI でもあり、
  # 版が固定されるので `npx shadcn@latest`（毎回違う版が降ってくる）を避けられる。
  shadcn: ^4.18.0
  # 日誌画面（`apps/web/app/routes/journal.tsx`）の双方向無限スクロール
  # （人間の依頼、2026-08-23）。React 19 対応・`Virtualizer` + `scrollRef` で
  # 既存のスクロール領域（`components/page.tsx` の内側 div）をそのまま親にでき、
  # スクロールバーが増えない。同種の候補（react-virtual 系）より依存が軽い。
  #
  # **0.50.4 ではなく 0.50.3 を採る。** 0.50.4 は 2026-08-22T08:44:59Z 公開で、
  # このエントリを足した時点（2026-08-22T17:19Z 実測）で公開から約8.5時間しか
  # 経っておらず、pnpm 11 既定の `minimumReleaseAge: 1440`（24時間）に引っかかる。
  # **`minimumReleaseAgeExclude` に足して通すのは採らない** — あの除外リストは
  # Claude 本体（SDK）を遅らせないためのものであって、一般の依存の新しい版を
  # 素通しするための場所ではない（同ファイル末尾の注記）。上の `swr` が
  # 同じ理由で枯れた版に留めている先例に揃える。
  virtua: ^0.50.3

# **Claude 本体（SDK）だけは、公開直後でも取りに行く。**
#
# pnpm 11 の既定は `minimumReleaseAge: 1440` — 公開から24時間経っていない版は
# 解決しない。一般の依存にはこの待ちが防御として効くが、SDK にかけると
# 「人間の手元の Claude Code より古いものをクローンとマネージャーに配る」ことに
# なり、その遅れがそのままデグレードになる（Dockerfile で `gh` の版を固定しない
# のと同じ理由）。SDK は毎日のように出るので、24時間の待ちは実質「常に1版古い」
# を意味する。
#
# **版番号を書かないこと。** `@0.3.228` の形は「その版だけを除外する」意味なので
# （pnpm の `parseVersionPolicyRule`）、上げるたびに9行を手で書き直す必要があり、
# 書き直し忘れた回だけ静かに24時間待たされる。名前パターン1本なら書き直す場所が
# 無い。pnpm は `*` を含む名前パターンを受け付ける（版を併記したときだけ
# `NAME_PATTERN_IN_VERSION_UNION` で拒否する）ので、この1行で本体と
# プラットフォーム別パッケージ8つの全部に効く。
#
# 版そのものを上げるのは `.github/workflows/update-claude-sdk.yml`（毎日）。
minimumReleaseAgeExclude:
  - '@anthropic-ai/claude-agent-sdk*'

# Dependabot alert 対応（9件）。`fast-uri` と `qs` はどちらも直接依存ではなく、
# 推移的依存としてしか入っていない —— 版を指定する場所が他に無いので `overrides`
# を新設する。
#
# - `fast-uri`: `ajv` の推移的依存で、`ajv` 側の宣言は `^3.0.1`。3.1.6 はその範囲に
#   入るので、上書きしても `ajv` は壊れない。**`ajv` を連れてくるのは
#   `@modelcontextprotocol/sdk`**（`@anthropic-ai/claude-agent-sdk` 経由と `shadcn`
#   経由の2本）**と `conf`**（`@dotenvx/dotenvx` ← `shadcn`）である。`pnpm why ajv -r`
#   で引き直せる。
# - `qs`: `express` / `body-parser` の推移的依存で、宣言はそれぞれ `^6.14.0` /
#   `^6.15.2`。6.16.0 はどちらの範囲にも入る。**`express` を連れてくるのも
#   `@modelcontextprotocol/sdk` である。**`hono` 側の経路ではない —— `hono` は自前の
#   クエリパーサを持つので `qs` を使わない。`pnpm why qs -r` で引き直せる。
#
# **置き場所はここ（`pnpm-workspace.yaml` の `overrides`）であって、root の
# `package.json` の `"pnpm": { "overrides": ... }` ではない。** pnpm 11 では後者は
# 読まれない（配布コード `pnpm.mjs` の `MIGRATED_PNPM_FIELD_KEYS` に `"overrides"`
# が入っており、書けば `The pnpm field in package.json is no longer read by pnpm`
# の警告になるだけで何も上書きしない）。**移したくなっても移さないこと** —
# `package.json` に `"pnpm"` フィールドは無いのが現状なので、新設もしないこと。
#
# **`js-yaml` は足した。`esbuild` はまだ足していない。**
# - `js-yaml`: 親が `@redocly/openapi-core@1.34.19` で、その宣言は `"js-yaml": "4.3.1"`
#   の厳密固定（範囲指定ではない、`npm view @redocly/openapi-core@1.34.19 dependencies`
#   で確認済み）。上書きはこの固定を強制上書きする形になるが、差は patch 1つ
#   （4.3.1 → 4.3.2）なので採った。`esbuild`（下記）とは形が違う —— あちらは宣言
#   レンジ `^0.27.0` の**外**へ出る。
#   実際の使用経路（`pnpm why js-yaml -r` で確認済み）は2本ある:
#   `@redocly/openapi-core` ← `openapi-typescript` ← `@alteroid/api-client`
#   （devDependencies、openapi.json を読んで型を生成する側）と、
#   `cosmiconfig` ← `shadcn` ← `@alteroid/web`（dependencies）。
#   **`pnpm build`（`pnpm -r build`）はこの2パッケージを両方ビルドするので、
#   緑であることがこの上書きの実挙動の検査になる。**
# - `esbuild`: 親の `tsup` の宣言が `^0.27.0` で、修正版（0.28.1）はその範囲に
#   入らない。上流（`tsup`）が `^0.28` を宣言し直すまでは見送り。
# **`hono` は `catalog:` と `overrides:` の両方に在る。重複ではない。** `catalog:` が
# 決めるのは消費側の宣言（`apps/cli` / `apps/daemon` / `apps/runner` の
# `"hono": "catalog:"`）だけで、**他人が自分で連れてくる `hono` には効かない。**
# `@modelcontextprotocol/sdk` は `hono` を `^4.11.4` として自分で宣言するので、
# catalog を上げても**そちら側は古い版のまま残る** —— 実測（catalog だけ上げた時点）:
# `pnpm why hono` が `Found 2 versions of hono` を返し、lockfile に `hono@4.13.1` が
# 5箇所残っていた。**そして残っていても `pnpm install` は成功し、CI も緑になる。**
# 消えないのは Dependabot の alert だけである。⟹ 推移的依存の側を動かすのは
# `overrides` である（4.13.5 は `^4.11.4` の内側なので、上書きしても SDK 側の宣言と
# 矛盾しない）。**片方だけ消さないこと** —— `catalog` を消すと消費側の宣言が失われ、
# `overrides` を消すと SDK 側が古い版へ戻る。
overrides:
  fast-uri: ^3.1.6
  hono: ^4.13.5
  js-yaml: ^4.3.2
  qs: ^6.16.0

# #246: `pnpm --filter <存在しないパッケージ名>` が黙って exit 0 を返す穴を塞ぐ。
# `test` script の有無とは無関係の別穴（パッケージ名の誤字・コピペミス）で、
# 実測（2026-08-23T07:3xZ）: `pnpm --filter @alteroid/nope test` は
# 「No projects matched...」と表示しつつ exit 0 だった。これを立てると exit 1 になる。
#
# **置き場所は `.npmrc` ではなくここ（`pnpm-workspace.yaml`）であること。**
# `.npmrc` に `fail-if-no-match=true` を置いても黙って無視される（実測
# 2026-08-23T07:36Z, pnpm 11.21.0）:
#   $ pnpm --filter @alteroid/nope --fail-if-no-match test    → EXIT:1
#   $ .npmrc に fail-if-no-match=true → 同じコマンド           → EXIT:0（無視される）
#   $ ここ（pnpm-workspace.yaml）に failIfNoMatch: true → 同じコマンド → EXIT:1（効く）
# `.npmrc` へ「移したほうが素直」に見えても移さないこと — 移した瞬間にこの穴が戻る。
#
# **⚠️ ここが塞ぐのは「その名前がワークスペースのどこにも無い」場合だけである。**
# 名前がどこかには在って、絞り込んだ先のパッケージには無い場合は、いまも exit 0 で
# 黙って通る。実測（2026-08-23T07:56Z, pnpm 11.21.0）:
#   $ pnpm --filter @alteroid/web zzz-nonexistent-script   → EXIT:1（どこにも無い）
#   $ apps/web/package.json から scripts.test を外して
#     pnpm --filter @alteroid/web test                     → EXIT:0（他の7つには在る）
# 9個目のワークスペースが `test` を書き忘れると、そのパッケージへの
# `pnpm --filter <新パッケージ> test` は黙ったまま exit 0 になる —— この設定では
# 直せない（pnpm の挙動そのもの）。捕まえるのは root の `pnpm test` に含まれる
# `scripts/workspace-test-scripts.test.ts` だけである。
#
# 変異試験の文脈での同じ話（`pnpm --filter` を変異試験で使うときにこの罠を踏む形）は
# `.claude/skills/mutation-testing/SKILL.md` に在る。**片方だけ直して終わらせないこと**
# — この設定の挙動が変わったら、あちらの実測も古くなる。
failIfNoMatch: true
