## 段0（測る）の続き — コード側から確定したこと

**観測 2026-09-11T12:37〜14:0xZ。** 本番 DB の実数は依頼者（クローン）が読み取り専用の `SELECT` で取得した一次情報（観測 2026-09-11T12:53〜12:56Z）であり、**調査側の器からは本番へ到達できない**（`ALTEROID_DATABASE_URL`/`DATABASE_URL`/`PGHOST` すべて unset、`psql`/`railway`/`pg_dump`/`docker` すべて不在）。これは PR #827「runner は自分の env から鍵を拾わない」が効いている姿であって、欠陥ではない。

以下、コードからの断定には `ファイル:行` を付ける。**推論と実測を分けてある。**

### 0. 先行調査の位置づけ

`investigate/698-archive-stage1`（`0d37b476`、2026-09-10T10:55Z）に `STAGE1-698-FINDINGS.md` が在るが、**PR #775 のマージ（同日 13:24Z）より前**に書かれている。その中核「`TranscriptArchive` に消す口が無い」は #775 で解消済みで、**現状の記述としては古い**。

### 1. なぜ生ログ1本が 100MB 級になるのか — **大きさの問題ではなく、重複の問題**

- `clone.ts:5464` `#onPreCompact` が `readFile(transcriptPath,'utf8')` で**ファイル全文**を読んで `archive()` へ渡す。差分でも末尾でもなく、**compaction のたびに毎回**。
- リポジトリ自身の逐語（`clone.ts:7114-7116`）: 「クローンの生ログは 1 本のセッションが伸び続ける形である —— resume は同じセッションへ書き足すので、ファイルは開始からの累積の全量を持つ。」
- マネージャー側も同型（`runner.ts:1580-1592` `#readTranscript` → `runner.ts:3762-3774` `#shipArchive` → `manager.ts:7032`）。

⟹ **k 回目の退避は (k-1) 回目を丸ごと含む。** N 回積めば一意な情報は最後の1本だけで、無駄は O(N²)。

**さらに、クローンとマネージャーで非対称がある:**

| | クローン | マネージャー |
|---|---|---|
| 退避のきっかけ | PreCompact のみ | PreCompact（`runner.ts:3741`）＋ **`stop()` のたび**（`runner.ts:1617`。正常停止・`manager_stop`・器の入れ替えが全部通る）＋ `#finish()`（crash/lost/failed、`runner.ts:2965`） |
| 鎖を切る弁 | **在る** — `#recycleForContextWindow` が `setCloneSessionId(null)` を打ち次は別 id（`clone.ts:3481`, `900-919`） | **無い**。`runner.ts:2053-2055` 逐語: 「会話は切らない。`#recycleForContextWindow`（クローン側の対）と違い、こちらは `sessionId` をそのまま渡して resume で同じ会話を続ける」 |

⟹ 同じ `managerId` の下で、器が入れ替わるたびに累積全量がもう1本積まれ、切れる箇所が無い。

**傍証（証明ではない）**: 実測の上位10本が 96.7〜103.4MB に密集している。独立した別々の記録なら散らばるはずで、1本の伸びるファイルを繰り返し写した形のほうがこの密集をよく説明する。

⚠ **本番データでの確認はまだ**。同一 `session_id` の複数行が実際に前方一致（prefix）関係にあるかは、読み取り専用の `SELECT` で検証予定。

### 2. 古い数字と新しい数字は単位が違う

Issue 本文（2026-09-08）の `sum(length(body))` は**展開後の文字数**。今回の測定は TOAST チャンク＝**圧縮後のディスク上のバイト数**。⟹ 「1件 120MB」（09-08）と「max 103.4MB」（09-11）は**比較できない**。行が縮んだのでも消されたのでもない（tombstone 0件と整合）。同じ理由で「95% → 89.2%」も改善ではなく測り方の違いを含む。

### 3. `removed_at` が 700件中0件である理由 — 「壊れている」のではなく「手で押す道具しか無い」

自動の掃除は**存在しない**。`prune` / `retention` / `ttl` / `sweep` / `vacuum` / `maxBytes|maxSize|quota` / workflow の `cron:` は **archive に対して全部0件**。`remove()` の呼び手は `archive_remove`（`tools.ts:6490`）と `DELETE /archive/:id`（`apps/daemon/src/app.ts:4184`）の2つだけで、どちらも人間／クローンの明示操作。

`guardArchiveRemoval`（`manager.ts:1234-1247`）が守るのは「**走行中**の委譲の `job.archiveIds` に在る行」だけで、齢もサイズも重要度も見ない。⟹ `#onPreCompact` 由来の行は `job.archiveIds` に入らないため**保護対象にすらならない**（常に `allowed`）。

⚠ pg の `remove()` は `UPDATE ... SET body=''`（`storage-pg/src/archive.ts:84-93`）＝ tombstone。MVCC の旧タプルは autovacuum が回るまで残るので、**消した直後に DB が縮むとは限らない**（未実測）。

### 4. 観測の口が無い（これは何も消さずに直せる）

- `GET /archive` の応答型は `z.object({ entries: z.array(z.string()) })`（`apps/daemon/src/openapi.ts:1205`）＝ **id の文字列配列だけ**。バイト数も時刻も返らない。
- `GET /archive/:id` は**本文を全文返す**（`app.ts:4088-4126`）。大きさだけを問う方法が無い。
- バイト数が返るのは**既に消した行**（410 応答）のみ。CLI も Web UI も同じ。`/stats` 相当は無い。

⟹ **サンクションされた口だけを使う限り、archive の大きさは誰にも測れない。** 段0 が本番へ直接降りる以外の方法で実行不能だったのはこのため。

### 5. 消す以外の手と、それぞれの代償

| 手 | 減る量 | 代償 | 価値判断が要るか |
|---|---|---|---|
| **重複を畳む**（同一 session_id は最新1本だけ残す） | 未確定（測定予定） | 中間スナップショットが消える。prefix が真なら最新が中間を全部含むので**情報は失われない** | **要らない**（消すのは*写し*であって*記録*ではない） |
| 孤児を落とす（`#onPreCompact` 由来で誰も id を保持しない行） | 未測定 | 生 id を知る者しか読めない行が読めなくなる（到達経路は事実上無い） | 少し要る |
| gzip して `bytea` | 約 1.83x ぶんだけ | 既に TOAST で約 4.6x 圧縮済み（先行調査の PGlite 実測）。`substr` の部分読みが効かなくなり読む側が重くなる | 要らないが**割に合わない** |
| DB の外（オブジェクトストレージ）へ | ほぼ全部 | 新しい依存・鍵・障害点が増える。秘密の管理面が増える | 要らないが別の危険 |
| 書く時点で間引く（末尾だけ退避） | 大きい | `manager_transcript` は全文を 8,000字ずつ辿る（`manager.ts:3759`）⟹ **遡れる範囲そのものが縮む** | **要る** |
| 齢（保持期間）で切る | 小さい | 古い行の大半は小さい（中央値 510KB）。消しても効かず、いちばん遡りたいものから失う | 要る |
| 総量の上限 | 可変 | 追い出す順の決め方に同じ価値判断が戻る | 要る |

**「大きいものから消す」は推さない。** 大きい行ほど長く粘った調査の記録である可能性を否定できないため、「最も価値の高い記録から消す」ことにならない理由を書けない。推せるのは**重複を畳む**ほうで、これは価値の順序を一切問わない。

### 6. journal / session_entries（併せて確認。消す提案はしない）

- `journal` は fs も pg も append-only で上限が**両方に無い**。`storage-pg/src/journal.ts:20-23` が意図を明言（「書き換えの口を用意しないのは fs 版と同じ理由…後から消せる形にしない」）⟹ 欠落ではなく**決定**。
- `session_entries` は pg のみ（fs 実装なし）。TTL も掃除も無い。`PgSessionStore.delete()` は在るがこの repo 内に呼び出し元が無い。
- `commitments` の「fs にだけ保持上限」（#468）は**意図された非対称**でテストに固定（`storage-pg/src/index.test.ts:1884-1893`）。
- ⟹ archive / journal / session_entries に #468 と同じ非対称は**無い**。共通するのは「pg へ移したときに無限成長を止める仕組みを誰も足していない」というより広い形。

### まだ確かめていないこと

- 同一 `session_id` の行が**実際に**前方一致関係にあるか（本番データでの確認が要る）
- 日ごとの**バイト数**（行数は測れているが、いまの増加速度はバイトでしか出ない）
- マネージャー由来とクローン由来の内訳（`session_id` の `mgr-` 前置で機械的に割れる見込み）
- `UPDATE` 後に autovacuum がどれだけ速やかにディスクを解放するか
