# Issue #698 段2 — 設計（⛔ 実装していない）

**この文書は実装ではない。** 依頼者（クローン）が「何をどれだけ消すか」の判断をオーナーへ上げるための素案である。
観測時刻はすべて UTC。本番の実数は依頼者が読み取り専用の `SELECT` で取得した一次情報（観測 2026-09-11T15:0xZ）で、
**この文書を書いた側は本番 DB へ到達できない**（設計上の非対称。PR #827 参照）。

## 前提（本番実測、2026-09-11T15:0xZ）

- `archive` の値の合計 5,897,862,888 バイト（圧縮後）。**`recoverable_pct = 92.3%`**
- 最大のセッション `715b0875-…`: **68行 / 4.39GB**、2026-09-03T03:02Z → 2026-09-08T01:43Z でほぼ一直線に伸びる
- **前方一致検査は真**（最大行と2番目の行で `small_is_prefix_of_big = true`、実測）
- ⚠ `nondecreasing = false`（2箇所でわずかに減っている）。**それでも prefix は真だった** ⟹ **単調性は重複の証拠にも反証にもならない。証拠は prefix 検査だけ**
- 行数では `mgr-` 由来 579行/296MB、クローン由来 126行/**5,601MB**

## 機序（コードから確定）

- `clone.ts:5464` `#onPreCompact` が `readFile(transcriptPath,'utf8')` で**全文**を読み、compaction のたびに無条件で積む
- リポジトリ自身の逐語（`clone.ts:7114-7116`）:「クローンの生ログは 1 本のセッションが伸び続ける形である —— resume は同じセッションへ書き足すので、ファイルは開始からの累積の全量を持つ。」
- ⟹ k 回目の退避は (k-1) 回目を丸ごと含む。無駄は O(N²)

### 弁（`#recycleForContextWindow`）は壊れていない

`#recycleForContextWindow = true` を打つ行は**コード全体で1箇所**（`clone.ts:3690`）。条件は「このターンが文脈超過の特定の失敗文言で落ちたこと」だけ
（`context-window-failure.ts:178-188` の3パターン）。`case 'compaction':`（`clone.ts:6274-6289`）は `#turn.compactions.push()` を打つだけで、
`#reportFailure` も `#noteContextWindowFold` も呼ばない。**compaction 成功から弁へ至る経路は存在しない。**

⟹ **「compaction が何回起きても弁は発火しない」は構造である。** 弁は「文脈があふれて*失敗した*」ときの復旧弁であって、
「セッションが長くなりすぎた」を見る弁ではない。**修正の場所は弁の条件ではない。**

## 正典との関係（⛔ `docs/` は1文字も変えていない）

> `docs/architecture.md:190` — 監査は日誌＋アーカイブで担保する。PreCompact フックで要約に潰す直前の**全文**をアーカイブへ落とす。
>
> `docs/PRD.md:122` — **セッションログ** | 各セッションの生のトランスクリプト（compaction で潰される前の**全文アーカイブ**を含む）

⟹ **「退避を差分にする」「末尾だけ積む」形の書き側修正は、正典と正面から衝突する。その道は採らない。**

🔑 **ただし正典が要求しているのは「compaction のたびに全文を*落とす*こと」であって、「過去の写しを*全部残す*こと」ではない。**
PRD の書き方（「各セッションの生のトランスクリプト」）は、むしろ **1セッション1本**を示唆している。

⚠ **この解釈は未確認である。** 依頼者にもオーナーにも確認が取れていない。**この解釈が通らないなら、書き側では直せない。**

## 設計A — 同一セッション内で「畳む」（⛔ 実装していない。解釈の確認待ち）

`archive(sessionId, body)` が呼ばれたとき、**同じ `sessionId` の直前の行を、新しい行が来た時点で tombstone する。**
積み直しをやめるだけで、退避の中身は全文のまま ⟹ 正典を1文字も変えずに O(N²) が O(N) になる。

### ① 読み手が壊れないこと

`Manager#transcript()` は `job.archiveIds` を**新しい順**に辿り、`removed` に当たったら次へフォールスルーする（`manager.ts:3759-3767`、PR #775 で入った）。
畳んでも**最新が全部を含む**ので、読み手は最新に当たって全文を得る。⟹ **差分では*ない*ので、読む側に組み直す責任は移らない。**
これが差分案より重要な利点である。

⚠ 例外を1つ明示する: **`TranscriptGrave.archiveId`（`store.ts:1054`）が指す行は畳まない。**

### ② prefix が崩れたら赤くなる歯 ——「歯」だけでなく「走行時の門」にする

**テストは走行時の沈黙を防げない。** ⟹ 両方置く。

- `archive` に `body_chars` と `body_md5` を持たせる
- 畳む前に、**新しい body の先頭 `prev.body_chars` 文字の md5 が `prev.body_md5` と一致するか**を確かめる。
  新しい body は手元のメモリに在るので、**古い 100MB を DB から読み戻す必要はない**
- 一致 ⟹ 畳む。**不一致 ⟹ 畳まずに両方残し、日誌に「前提が崩れた」と名指しで書く**
- 歯は `verifyTranscriptArchiveContract()`（3実装を1つで縛る既存の仕組み）に2本:
  (a) prefix が真 → 前の行が畳まれる / (b) **prefix が偽 → 両方残る**。
  **(b) が、誰かが「常に畳む」に最適化したときに赤くなる歯である**
- コメントで名乗らせる内容: 「この設計は『新しい退避は古い退避を先頭に含む』という**外の世界（SDK の生ログの書き方）の性質**に
  寄りかかっている。SDK が書き方を変えれば前提は黙って崩れる。崩れたとき、**畳むのをやめて両方残す**。」

### ③ 単調性を前提にしない

見るのは長さの大小ではなく **md5 の一致だけ**。⟹ 本番で観測された非単調な2行でも、prefix が真なら畳め、偽なら畳まない。

## 設計B — 既存の 5.44GB を消す（⛔ 実装していない。オーナー判断）

**tombstone は `UPDATE ... SET body=''`（`storage-pg/src/archive.ts:84-93`）＝ 取り返しのつかない操作である。**

1. **実行直前にその場で測り直す。** 道具は**下見（dry-run）を既定**にし、対象の行数・バイト数・セッション数をその場の1トランザクションで数えて返す。
   押すときは**その下見が返した件数とバイト数を呼び手が書き戻す**まで実行しない（数が動いていたら拒否）。⟹ 過去に測った数は一切使わない
2. **1件ずつ確かめてから消す。** 各候補行について、同一セッションの最新行に対して
   `md5(substr(newest,1,len(candidate))) = md5(candidate)` を確かめ、**真のものだけ tombstone。偽なら飛ばして記録。** まとめて信じない
3. **間違えたときに失われるもの。** prefix の確認を誤った行を消すと、**その退避にしか無かった区間が永久に消え**、
   「報告と現物の食い違い」を掘れる範囲がその窓のぶんだけ縮む。救いは1つ —— tombstone は行を残すので
   **「そこに何バイトのものが在って、いつ消したか」は残る**（`removed_at` / `removed_bytes`）。⟹ 沈黙ではなく欠落として見える
4. **途中で落ちたら。** **1行1トランザクション、`WHERE removed_at IS NULL` で冪等、再開可能。** 一括 UPDATE にしない。
   途中で落ちても「確認済みのものだけが消えた一貫した状態」で止まる
5. **安全弁。** セッションの**最新行は決して対象にしない** / 走行中の委譲が参照する行は既存の `guardArchiveRemoval` で拒否 /
   1回あたりの上限 / 理由を日誌へ必ず残す

⚠ **容量は即座には戻らない。** tombstone は `UPDATE` なので MVCC の旧タプルが残り、**autovacuum が回るまでディスクは空かない。**
完全に返すには `VACUUM FULL`（ロックを取る）か `pg_repack` が要る。⟹ 「5.44GB 消えた」を期待すると裏切られる。

## 推さない手（理由付き）

| 手 | なぜ推さないか |
|---|---|
| **大きいものから消す** | **「それが最も価値の高い記録から消すことにならない」理由を書けない。** 大きい行ほど長く粘った調査である可能性を否定できない |
| 齢（保持期間）で切る | 古い行の大半は小さい（中央値 510KB）。消しても効かず、いちばん遡りたいものから失う |
| gzip して `bytea` | 既に TOAST で約 4.1x 圧縮済み。上積みは約 1.83x だけ。`substr` の部分読みが効かなくなり読む側が重くなる |
| DB の外（オブジェクトストレージ） | 新しい依存・鍵・障害点が増える。秘密の管理面が増える |
| 書く時点で間引く（末尾だけ） | 正典（`architecture.md:190` / `PRD.md:122`）と衝突する。かつ `manager_transcript` の遡れる範囲が縮む |

## 別件（この作業で見つけた欠陥。PR #842 で直していない）

**`archive()` は、同じセッションへ同じミリ秒に2回積むと片方を黙って捨てる。**
id は `${sanitize(sessionId)}-${stamp}.jsonl` で `stamp` はミリ秒精度 ⟹ 衝突し、pg は `onConflictDoUpdate`、fs は `writeFile` で上書きする。
**`origin/main` に元から在る**（PR #842 より前）。契約テストで3回連続に積んで実際に踏んだ（行が2本しかできず、本文が1本上書きされた）。

帰結: ⑴ `rows` が**過少に数えうる**（本番の「68回」は下限かもしれない）。⑵ **生ログが黙って1本消えうる。**
⟹ **Issue として切り出す価値が在る。** id の形を変えると fs 側の `STAMP_SUFFIX_RE`（id から `sessionId`/`at` を復元する）も一緒に変わるため、範囲が別物になる。
